name: Testing

on:
    # push:
    #     branches: [main]
    workflow_dispatch: 



jobs:
    Test:
        runs-on: ubuntu-latest
        steps: 
            - name: Running a test
              run: exit 1
