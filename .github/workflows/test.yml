name: Hello Workflow

on:
  push:

jobs:

  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - name: Print Message 
        run: echo "Hello from GitHub Actions"